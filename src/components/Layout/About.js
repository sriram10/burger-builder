import React from 'react'

const about = (props) => {
  console.log(props, props.match.params)
  return (
    <div>
      <h1>About Component</h1>
      <p>Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      </p>
      <hr/>
      <div id="sometag">
        <h2>Some Tag content</h2>
        <p>Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      </p>
      </div>
      <div id="one">
        <h2>ONE</h2>
        <p>Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      </p>
      </div>
      <div id="two">
        <h2>Two</h2>
        <p>Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      </p>
      </div>
      <div id="three">
        <h2>Three</h2>
        <p>Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
        Components are self-sustaining, independent micro-entities that describe a part of your UI(like navigation header, drop-down, slider, form,…). An application's UI can be split up into smaller components where each component can have its own code, structure, and API.
      </p>
      </div>
    </div>
  )
}

export default about